using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.EventSystems;

public class ScrollButton : MonoBehaviour   //, IPointerDownHandler
{
    /*
    #region
    public ScrollRect scrollObeject;
    public Image scrollButton;
    public bool up;
    public Image backgroundHeight;
    #endregion

    [SerializeField]
    private GameObject[] menu;
    private AudioSource audio;

    private float height;
    private float space;
    private RectTransform scroll;


    private void Start()
    {
        scroll = scrollObejct.content.GetComponent<RectTransform>();
        height = backgroundHeight.rectTransform.rect.height;
        space = scroll.GetComponent<VerticalLayoutGroup>().spacing;

        StartCoroutine(scolling(0, false));
    }

    public void OnPointerDown(PointerEventData eventData)
    {

    }
    */
}
